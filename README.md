# dotfiles

these are my .config files. It's wip so things will probs shift around

## Dependencies

Install system dependencies with Homebrew:

```sh
brew bundle --file=~/.config/brew/Brewfile
chsh -s $(which fish)
```
